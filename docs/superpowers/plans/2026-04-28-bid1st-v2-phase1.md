# Phase 23-9 1단계 — recommendBid1st 통계 추정 + UI 노출 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 자사 1위 낙찰 추천 시스템 1단계 — `bid_records` 53K 통계 기반 1위 사정률 분포 추정 → 0.0001% 정밀도 grid search로 자동 추천 위치 산출 → 적격성 라벨링과 함께 UI에 노출.

**Architecture:** `predictV5`(시장 분포 추정)는 유지하고 `recommendBid1st`(신규)가 메인 추천 담당. `bid_predictions`에 6개 컬럼 추가 + `win1st_dist_map`/`bid1st_v2_validation` VIEW 신설. `getFinalRecommendation`은 어댑터화 (`bid1st_v2_adj` 우선, 미존재 시 기존 `opt_adj` 폴백). `calcWin1stBid`/`WIN_OPT_GAP`은 1단계에선 코드 유지(폴백용), 3단계에서 제거.

**Tech Stack:** React + Vite + Supabase Postgres (REST), JS 단일 파일 구조(App.jsx 2500줄), Phase 23-3 deploy-gate.

**Spec 참조:** `docs/superpowers/specs/2026-04-28-bid1st-recommendation-redesign-design.md` (커밋 `78ef7d7`)

**범위:** spec 1단계 작업 1.1~1.10 + Phase 23-3 게이트 + main push까지. 측정·통과 판정(4주)은 별도 plan.

**테스트 전략:** bid-analyzer는 단위 테스트 인프라 부재. 다층 검증으로 대체:
1. 매 코드 변경 후 `npx vite build` 통과
2. DB 마이그레이션 직후 데이터 검증 쿼리
3. 통합 검증: dev server에서 수동 시뮬레이션 호출 + DB 확인
4. /evaluate 회귀 검증 (`opt_adj` 무변경 → mae PASS 자동 기대)
5. deploy-gate 통합 게이트

---

## File Structure

새 파일: 없음. 기존 파일에 추가만.

| 파일 | 변경 내용 | 책임 |
|---|---|---|
| `src/lib/utils.js` | `calcWinProb`, `lookupWin1stDist`, `recommendBid1st` 추가 (끝에) | 추천 로직 (Generator) |
| `src/lib/supabase.js` | `sbFetchWin1stDistMap` 추가, `sbSavePredictions` 컬럼 매핑 확장 | DB 어댑터 |
| `src/App.jsx` | `useState`·`useEffect`·`loadPredFiles`·`doManualPred`·`getFinalRecommendation`·UI 변경 | 통합 + UI |
| Postgres | `bid_predictions` ALTER + `win1st_dist_map` VIEW + `bid1st_v2_validation` VIEW | 데이터 모델 |

---

## Task 1: DB 컬럼 추가 (`bid_predictions`)

**Files:** Postgres (no source file)

- [ ] **Step 1: ALTER 실행 (Supabase MCP)**

```sql
ALTER TABLE bid_predictions
  ADD COLUMN IF NOT EXISTS bid1st_v2_adj         numeric,
  ADD COLUMN IF NOT EXISTS bid1st_v2_bid         numeric,
  ADD COLUMN IF NOT EXISTS bid1st_v2_win_prob    numeric,
  ADD COLUMN IF NOT EXISTS bid1st_v2_floor_safe  boolean,
  ADD COLUMN IF NOT EXISTS bid1st_v2_grain       text,
  ADD COLUMN IF NOT EXISTS bid1st_v2_src         text;
```

- [ ] **Step 2: 컬럼 추가 확인**

```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name='bid_predictions'
  AND column_name LIKE 'bid1st_v2_%'
ORDER BY column_name;
```

Expected: 6개 row 반환 (`bid1st_v2_adj numeric`, `bid1st_v2_bid numeric`, `bid1st_v2_floor_safe boolean`, `bid1st_v2_grain text`, `bid1st_v2_src text`, `bid1st_v2_win_prob numeric`).

---

## Task 2: `win1st_dist_map` VIEW 생성

**Files:** Postgres

- [ ] **Step 1: VIEW 생성 SQL 실행**

```sql
CREATE OR REPLACE VIEW win1st_dist_map AS
WITH src AS (
  SELECT
    at, canonical_ag,
    CASE
      WHEN ba < 1e8  THEN 'S1'
      WHEN ba < 3e8  THEN 'S2'
      WHEN ba < 1e9  THEN 'S3'
      WHEN ba < 3e9  THEN 'S4'
      ELSE                'S5'
    END AS ba_seg,
    (br1 - 100) AS adj
  FROM bid_records
  WHERE br1 IS NOT NULL
    AND br1 BETWEEN 95 AND 105
    AND ba IS NOT NULL AND ba > 0
    AND at IS NOT NULL AND canonical_ag IS NOT NULL
    AND co IS NOT NULL AND co NOT LIKE '%유찰%'
    AND is_excluded IS NOT TRUE
    AND od >= '2024-01-01'
)
SELECT 'AG_BA' AS grain, canonical_ag AS key1, ba_seg AS key2,
       COUNT(*)::int AS n,
       ROUND(AVG(adj)::numeric, 4) AS mean,
       ROUND(stddev_samp(adj)::numeric, 4) AS std
FROM src GROUP BY canonical_ag, ba_seg HAVING COUNT(*) >= 5
UNION ALL
SELECT 'AG', canonical_ag, NULL,
       COUNT(*)::int, ROUND(AVG(adj)::numeric, 4), ROUND(stddev_samp(adj)::numeric, 4)
FROM src GROUP BY canonical_ag HAVING COUNT(*) >= 5
UNION ALL
SELECT 'AT_BA', at, ba_seg,
       COUNT(*)::int, ROUND(AVG(adj)::numeric, 4), ROUND(stddev_samp(adj)::numeric, 4)
FROM src GROUP BY at, ba_seg HAVING COUNT(*) >= 5
UNION ALL
SELECT 'AT', at, NULL,
       COUNT(*)::int, ROUND(AVG(adj)::numeric, 4), ROUND(stddev_samp(adj)::numeric, 4)
FROM src GROUP BY at HAVING COUNT(*) >= 5;

COMMENT ON VIEW win1st_dist_map IS
  'Phase 23-9: 1위 사정률 분포 다단 fallback (AG_BA → AG → AT_BA → AT). bid_records 53K 기반.';
```

- [ ] **Step 2: 셀 커버리지 측정**

```sql
SELECT grain, COUNT(*) AS cells, SUM(n) AS total_obs
FROM win1st_dist_map GROUP BY grain ORDER BY grain;
```

Expected: 4 grain (AG_BA / AG / AT_BA / AT) 모두 행 반환. AG_BA cells ≥ 50, AT cells ≥ 5.

- [ ] **Step 3: AG_BA 직접 hit 비율 추정**

```sql
WITH bp_recent AS (
  SELECT canonical_ag, at,
    CASE WHEN ba < 1e8 THEN 'S1' WHEN ba < 3e8 THEN 'S2'
         WHEN ba < 1e9 THEN 'S3' WHEN ba < 3e9 THEN 'S4' ELSE 'S5' END AS ba_seg
  FROM bid_predictions
  WHERE created_at >= CURRENT_DATE - INTERVAL '30 days'
    AND canonical_ag IS NOT NULL AND ba IS NOT NULL
)
SELECT
  COUNT(*) AS total,
  COUNT(*) FILTER (
    WHERE EXISTS (SELECT 1 FROM win1st_dist_map m
      WHERE m.grain='AG_BA' AND m.key1=bp_recent.canonical_ag AND m.key2=bp_recent.ba_seg)
  ) AS ag_ba_hits
FROM bp_recent;
```

Expected: `ag_ba_hits / total ≥ 0.30` (30%+). 미달 시 STOP — spec 11.3 운영 위험 시나리오 (셀 커버리지 부족) 적용 → grain 정의 재검토 후 plan 수정.

- [ ] **Step 4: 커밋 (DB 변경은 git에 안 들어감 — 다음 task로)**

DB 변경은 별도 커밋 없음. Postgres에 즉시 반영.

---

## Task 3: `bid1st_v2_validation` VIEW 생성

**Files:** Postgres

- [ ] **Step 1: VIEW 생성 SQL 실행**

```sql
CREATE OR REPLACE VIEW bid1st_v2_validation AS
SELECT
  p.id, p.at, p.ag, p.canonical_ag, p.open_date, p.match_status,
  p.ba, p.ep, p.av,
  p.bid1st_v2_adj         AS adj,
  p.bid1st_v2_bid         AS bid,
  p.bid1st_v2_win_prob    AS predicted_win_prob,
  p.bid1st_v2_floor_safe  AS floor_safe,
  p.bid1st_v2_grain       AS grain,
  p.bid1st_v2_src         AS src,
  p.pred_floor_rate       AS fr,
  p.actual_bid_amount     AS winner_bid,
  p.actual_expected_price AS winner_xp,
  p.actual_adj_rate,
  CASE WHEN p.actual_expected_price IS NOT NULL THEN
    p.bid1st_v2_bid > p.actual_bid_amount
    AND p.bid1st_v2_bid <= p.actual_expected_price
  END AS closer_than_winner,
  CASE WHEN p.actual_bid_amount > 0 THEN
    (p.bid1st_v2_bid - p.actual_bid_amount)::numeric / p.actual_bid_amount * 100
  END AS bid1st_pct_gap,
  CASE WHEN p.actual_expected_price IS NOT NULL THEN
    (p.bid1st_v2_bid > p.actual_bid_amount
     AND p.bid1st_v2_bid <= p.actual_expected_price)::int
  END AS actual_won
FROM bid_predictions p
WHERE p.match_status='matched'
  AND p.bid1st_v2_bid IS NOT NULL
  AND p.actual_bid_amount IS NOT NULL;

COMMENT ON VIEW bid1st_v2_validation IS
  'Phase 23-9 검증: recommendBid1st 자동 추천의 1위 적중률·적격성·win_prob calibration.';
```

- [ ] **Step 2: 빈 결과셋 확인 (아직 bid1st_v2_bid 채워진 행 없음)**

```sql
SELECT COUNT(*) FROM bid1st_v2_validation;
```

Expected: 0. 코드 배포 + 신규 매칭 누적 후에만 행이 생김.

---

## Task 4: `src/lib/utils.js` — `calcWinProb` 헬퍼 추가

**Files:**
- Modify: `src/lib/utils.js` (파일 끝에 추가)

JS에 `Math.erf`가 없으므로 다항식 근사 사용 (Abramowitz & Stegun 7.1.26).

- [ ] **Step 1: utils.js 끝부분 확인**

```bash
wc -l src/lib/utils.js
```

Expected: 1000줄대 (현재 약 700~800줄대).

- [ ] **Step 2: 파일 끝에 헬퍼 + calcWinProb 추가**

`src/lib/utils.js` 마지막 줄 뒤에 추가:

```js

// ─── Phase 23-9: 자사 1위 낙찰 추천 (recommendBid1st) ──────────
// Abramowitz & Stegun 7.1.26 erf 근사 (max error 1.5e-7)
function _erf(x) {
  const sign = x < 0 ? -1 : 1;
  x = Math.abs(x);
  const a1=0.254829592, a2=-0.284496736, a3=1.421413741,
        a4=-1.453152027, a5=1.061405429, p=0.3275911;
  const t = 1 / (1 + p * x);
  const y = 1 - ((((a5*t + a4)*t + a3)*t + a2)*t + a1) * t * Math.exp(-x*x);
  return sign * y;
}

// 표준정규 CDF Φ(z)
function _phi(z) { return 0.5 * (1 + _erf(z / Math.SQRT2)); }

// 1위 확률: 자사 위치가 분포 평균보다 위에 있을수록 ↑, 적격성 미달 시 0
export function calcWinProb(adj, mean, effStd, floorSafe) {
  if (!floorSafe) return 0;
  if (!effStd || effStd <= 0) return 0.5;
  const z = (adj - mean) / effStd;
  return _phi(z);
}
```

- [ ] **Step 3: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`. 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/lib/utils.js
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 calcWinProb 헬퍼 추가 (erf 다항식 근사)

자사 1위 확률 산출용 표준정규 CDF. recommendBid1st의 winProb 계산에 사용.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `src/lib/utils.js` — `lookupWin1stDist` 헬퍼 추가

**Files:**
- Modify: `src/lib/utils.js` (`calcWinProb` 다음에 추가)

- [ ] **Step 1: lookupWin1stDist 함수 추가**

`utils.js`에서 `calcWinProb` 함수 정의 바로 뒤에 추가:

```js

// 다단 fallback lookup: AG_BA → AG → AT_BA → AT
// distMap: {agBa, ag, atBa, at} 각 객체는 sbFetchWin1stDistMap 결과
// baSeg: 'S1'~'S5' (predictV5와 동일 분할)
// 반환: {n, mean, std, grain, src} 또는 null
export function lookupWin1stDist(at, agName, baSeg, distMap) {
  if (!distMap) return null;
  const ag_ba_key = `${agName}|${baSeg}`;
  if (distMap.agBa && distMap.agBa[ag_ba_key]) {
    const v = distMap.agBa[ag_ba_key];
    return { ...v, grain: 'AG_BA', src: `${agName} ${baSeg}(${v.n}건)` };
  }
  if (distMap.ag && distMap.ag[agName]) {
    const v = distMap.ag[agName];
    return { ...v, grain: 'AG', src: `${agName}(${v.n}건)` };
  }
  const at_ba_key = `${at}|${baSeg}`;
  if (distMap.atBa && distMap.atBa[at_ba_key]) {
    const v = distMap.atBa[at_ba_key];
    return { ...v, grain: 'AT_BA', src: `${at} ${baSeg}(${v.n}건)` };
  }
  if (distMap.at && distMap.at[at]) {
    const v = distMap.at[at];
    return { ...v, grain: 'AT', src: `${at}(${v.n}건)` };
  }
  return null;
}

// ba_seg 분할 (predictV5와 동일)
export function baSegOf(ba) {
  const n = Number(ba) || 0;
  if (n < 1e8) return 'S1';
  if (n < 3e8) return 'S2';
  if (n < 1e9) return 'S3';
  if (n < 3e9) return 'S4';
  return 'S5';
}
```

- [ ] **Step 2: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 3: 커밋**

```bash
git add src/lib/utils.js
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 lookupWin1stDist + baSegOf 헬퍼 추가

다단 fallback (AG_BA → AG → AT_BA → AT) lookup. predictV5의 5구간(S1~S5)과 정합.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `src/lib/utils.js` — `recommendBid1st` 메인 함수 추가

**Files:**
- Modify: `src/lib/utils.js` (`baSegOf` 다음에 추가)

- [ ] **Step 1: recommendBid1st 함수 추가**

`baSegOf` 정의 바로 뒤에 추가:

```js

// ─── Phase 23-9 메인: recommendBid1st ───────────────────────────
// bid: {at, agName, ba, ep, av, fr}
// context: {distMap}  // 1단계는 distMap만 사용. bidDetails는 2단계.
// options: {gridStep, gridRange, minSamples, noiseFloor, enableMonteCarlo}
// 반환: {auto, scenarios, distribution} 또는 null
export function recommendBid1st(bid, context, options) {
  const opt = Object.assign({
    gridStep: 0.0001, gridRange: 1.5, minSamples: 5,
    noiseFloor: 0.642, enableMonteCarlo: false
  }, options || {});
  const { at, agName, ba, ep, av, fr } = bid || {};
  if (!at || !ba || !fr) return null;

  // Step 1: 분포 lookup (다단 fallback)
  const baSeg = baSegOf(ba);
  let dist = lookupWin1stDist(at, agName, baSeg, context?.distMap);
  let distSrc = dist ? dist.src : `시스템 기본(표본 부족)`;
  let distGrain = dist ? dist.grain : null;
  let mean = dist ? Number(dist.mean) : 0;
  let std  = dist ? Number(dist.std)  : 0.642;
  if (!dist || dist.n < opt.minSamples) {
    distGrain = null; mean = 0; std = 0.642;
    distSrc = '시스템 기본(표본 부족)';
  }

  // Step 2: 노이즈 floor 적용
  const effStd = Math.max(std, opt.noiseFloor);

  // Step 3: Monte Carlo는 2단계 (1단계 noop)
  // (enableMonteCarlo 분기 자리만 남김)

  // Step 4: Grid search 0.0001% 정밀도
  const xpC = (adj) => ba * (1 + adj/100);
  const bidC = (adj) => {
    const xp = xpC(adj);
    return (av && av > 0)
      ? Math.ceil(av + (xp - av) * (fr/100))
      : Math.ceil(xp * (fr/100));
  };
  // 적격성: bidC(adj) ≥ legal_min(adj). 두 식이 동일하므로
  // 자연스럽게 floorSafe = (xp ≥ av), 즉 ba(1+adj/100) ≥ av.
  // av=0이거나 av≪ba면 항상 true. 자격 미달은 av가 큰 LH 같은 case에서 발생.
  const floorSafeC = (adj) => {
    const xp = xpC(adj);
    return (!av || av <= 0) ? true : xp >= av;
  };

  const startAdj = mean - opt.gridRange;
  const endAdj   = mean + opt.gridRange;
  let bestAdj = mean, bestProb = -1;
  for (let adj = startAdj; adj <= endAdj; adj += opt.gridStep) {
    const fs = floorSafeC(adj);
    const wp = calcWinProb(adj, mean, effStd, fs);
    // tie-break: mean에 가까운 쪽 우선
    if (wp > bestProb || (wp === bestProb && Math.abs(adj - mean) < Math.abs(bestAdj - mean))) {
      bestProb = wp; bestAdj = adj;
    }
  }

  const r4 = (v) => Math.round(v * 10000) / 10000;
  const buildOption = (adj) => {
    const fs = floorSafeC(adj);
    const wp = calcWinProb(adj, mean, effStd, fs);
    return {
      adj: r4(adj), bid: bidC(adj),
      winProb: Math.round(wp * 100) / 100,
      floorSafe: fs,
      label: fs ? `자격OK·1위${Math.round(wp*100)}%` : '자격미달'
    };
  };

  // Step 5: 출력
  // P25/P75: 정규분포 분위수 ±0.6745 × effStd
  return {
    auto: buildOption(bestAdj),
    scenarios: {
      aggressive:   buildOption(mean - 0.6745 * effStd),
      balanced:     buildOption(mean),
      conservative: buildOption(mean + 0.6745 * effStd)
    },
    distribution: {
      grain: distGrain, n: dist?.n || 0,
      mean: r4(mean), std: r4(std),
      src: distSrc,
      monteCarloUsed: false
    }
  };
}
```

- [ ] **Step 2: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 3: 빠른 sanity 검증 — 노드에서 직접 호출**

```bash
node -e "
const { recommendBid1st } = require('./src/lib/utils.js');
const distMap = { agBa: {'한전|S2': {n:12, mean:0.45, std:0.31}}, ag:{}, atBa:{}, at:{} };
const r = recommendBid1st(
  { at:'한전', agName:'한전', ba:200000000, ep:200000000, av:0, fr:88.250 },
  { distMap }
);
console.log(JSON.stringify(r, null, 2));
"
```

Expected: `auto.adj`가 0.0001 정밀도 4자리, `auto.winProb` 0~1 사이, `auto.floorSafe = true`, `distribution.grain = 'AG_BA'`.

(주: ESM/CJS 호환성 이슈 시 이 step은 skip하고 dev server 검증으로 대체 — Task 14에서 처리)

- [ ] **Step 4: 커밋**

```bash
git add src/lib/utils.js
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 recommendBid1st 메인 함수 추가

다단 fallback 분포 lookup → 노이즈 floor 적용 → 0.0001% grid search →
1위 확률 최대 위치(auto) + 3구간(P25/P50/P75) + 적격성 라벨.
1단계: A 통계 추정만 (Monte Carlo 미적용).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `src/lib/supabase.js` — `sbFetchWin1stDistMap` 추가

**Files:**
- Modify: `src/lib/supabase.js` (`sbFetchPredBiasMap` 함수 정의 다음에 추가)

- [ ] **Step 1: 위치 확인**

```bash
grep -n "sbFetchPredBiasMap\|sbFetchFloorBench" src/lib/supabase.js
```

Expected: `sbFetchFloorBench`(line 257), `sbFetchPredBiasMap`(line 273) 등 위치 출력.

- [ ] **Step 2: `sbFetchPredBiasMap` 함수 정의 끝 다음에 신규 함수 삽입**

`src/lib/supabase.js`에서 `sbFetchPredBiasMap` 함수 종료 직후(다음 빈 줄 위치)에 추가:

```js

// ─── Phase 23-9: 1위 사정률 분포 다단 fallback map ──────────
// win1st_dist_map VIEW (AG_BA → AG → AT_BA → AT) → JS map
export async function sbFetchWin1stDistMap(){
  try{
    const res=await authedFetch("/rest/v1/win1st_dist_map?select=grain,key1,key2,n,mean,std&limit=2000");
    if(!res.ok)return{agBa:{},ag:{},atBa:{},at:{}};
    const rows=await res.json();
    const m={agBa:{},ag:{},atBa:{},at:{}};
    for(const r of rows){
      const v={n:Number(r.n),mean:Number(r.mean),std:Number(r.std)};
      if      (r.grain==='AG_BA') m.agBa[r.key1+'|'+r.key2]=v;
      else if (r.grain==='AG')    m.ag[r.key1]=v;
      else if (r.grain==='AT_BA') m.atBa[r.key1+'|'+r.key2]=v;
      else if (r.grain==='AT')    m.at[r.key1]=v;
    }
    return m;
  }catch(e){return{agBa:{},ag:{},atBa:{},at:{}}}
}
```

- [ ] **Step 3: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 4: 커밋**

```bash
git add src/lib/supabase.js
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 sbFetchWin1stDistMap 추가

win1st_dist_map VIEW를 4단 grain map으로 fetch.
sbFetchPredBiasMap 패턴과 동일.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `src/lib/supabase.js` — `sbSavePredictions` 매핑 확장

**Files:**
- Modify: `src/lib/supabase.js` (`sbSavePredictions` 함수 — bid_predictions INSERT 매핑)

- [ ] **Step 1: 현재 sbSavePredictions 위치 확인**

```bash
grep -n "sbSavePredictions" src/lib/supabase.js
```

- [ ] **Step 2: dbRows 매핑 코드 위치 확인 (App.jsx 569·623 line의 dbRows 객체와 정합)**

`src/lib/supabase.js`의 `sbSavePredictions` 함수 안에서 `body` 또는 `payload`를 만드는 부분 확인. 현재 호출자가 dbRows를 객체로 전달하고 그대로 INSERT 하는 구조. 이 함수 자체에 매핑이 없으면 변경 불필요 — 모든 매핑은 호출자(App.jsx)에서 처리.

```bash
sed -n '/export async function sbSavePredictions/,/^}/p' src/lib/supabase.js | head -40
```

- [ ] **Step 3: 호출자 컬럼 매핑은 Task 10에서 처리하므로 이 task에서 supabase.js 변경 없음**

이 task는 위치 확인만. 실제 컬럼 추가는 Task 10(`loadPredFiles` dbRows에서) + Task 11(`doManualPred` — 단 doManualPred는 DB 저장 안 함, App.jsx:665~673 주석 "DB 저장 안 함 — 시뮬레이션 전용").

따라서 **Task 8은 no-op (skip 처리)**. 실제 컬럼 매핑은 Task 10에서.

- [ ] **Step 4: Task 8 완료 처리 — 커밋 없음**

```bash
echo "Task 8: supabase.js 변경 불필요. sbSavePredictions가 dbRows를 그대로 INSERT하므로 호출자(App.jsx)에서만 컬럼 추가."
```

---

## Task 9: `src/App.jsx` — state + fetch 통합

**Files:**
- Modify: `src/App.jsx`

- [ ] **Step 1: 상단 import 확인 + 신규 함수 import 추가**

`src/App.jsx:9` 줄을 다음으로 교체 (`recommendBid1st`, `baSegOf` 추가):

```js
import { clsAg, clean, tc, tn, pDt, mSch, md5, parseFile, toRecord, toRecords, parseBidDoc, calcStats, predictV5, calcDataStatus, isSucviewFile, parseSucview, simDraws, pnv, sn, eraFR, isNewEra, isLhJongsim, sanitizeJson, recommendAssumedAdj, calcRoiV2, buildAiContext, callClaudeAi, WIN_OPT_GAP, calcWin1stBid, calcBenchmarkAdj, getBiasArrow, normalizeAgencyName, recommendBid1st, baSegOf } from "./lib/utils.js";
```

- [ ] **Step 2: supabase.js fetch 함수 import 추가**

`src/App.jsx:10` 부근의 supabase import 줄 찾아 `sbFetchWin1stDistMap` 추가.

```bash
grep -n "from \"./lib/supabase.js\"" src/App.jsx
```

해당 줄 import 목록 끝에 `sbFetchWin1stDistMap` 추가.

- [ ] **Step 3: useState 추가**

`src/App.jsx:202` (`floorBench` 라인) 바로 뒤에 추가:

```js
  const[win1stDistMap,setWin1stDistMap]=useState({agBa:{},ag:{},atBa:{},at:{}}); // Phase 23-9: 1위 사정률 분포 다단 fallback
```

- [ ] **Step 4: useEffect fetch 호출 추가**

`src/App.jsx:477` (`sbFetchFloorBench` 라인) 바로 뒤에 추가:

```js
    try{const wd=await sbFetchWin1stDistMap();if(wd)setWin1stDistMap(wd)}catch(e){}
```

- [ ] **Step 5: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 6: 커밋**

```bash
git add src/App.jsx
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 win1stDistMap state + fetch 통합

App.jsx에 useState[win1stDistMap] 추가, 초기 로드 시 sbFetchWin1stDistMap 호출.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `src/App.jsx` — `loadPredFiles`에 `recommendBid1st` 통합

**Files:**
- Modify: `src/App.jsx:606~644` (`loadPredFiles` 함수)

- [ ] **Step 1: 현재 items.map 라인 위치 확인**

```bash
grep -n "items.map(item=>{const p=predictV5" src/App.jsx
```

Expected: line 626 부근.

- [ ] **Step 2: items.map 부분 변경 — recommendBid1st 호출 추가**

`src/App.jsx:626` 부근의 다음 코드:

```js
const results=items.map(item=>{const p=predictV5({at:item.at,agName:item.ag,ba:item.ba,ep:item.ep,av:item.av},allS.ts,allS.as,bidDetails,agencyPred,floorBench);const rec=recommendAssumedAdj({at:item.at,agName:item.ag,ba:item.ba,ep:item.ep,av:item.av},allS.ts,allS.as,curAgAss);return{...item,pred:p,rec}}).filter(r=>r.pred);
```

다음으로 교체 (한 줄 → 멀티라인으로 풀어쓰기):

```js
const results=items.map(item=>{
  const p=predictV5({at:item.at,agName:item.ag,ba:item.ba,ep:item.ep,av:item.av},allS.ts,allS.as,bidDetails,agencyPred,floorBench);
  const rec=recommendAssumedAdj({at:item.at,agName:item.ag,ba:item.ba,ep:item.ep,av:item.av},allS.ts,allS.as,curAgAss);
  // Phase 23-9: 신규 추천 (1위 확률 최대 위치)
  const fr=p?p.fr:eraFR(item.at,item.ep||item.ba,new Date().toISOString().slice(0,10));
  const v2=recommendBid1st(
    {at:item.at,agName:item.ag,ba:item.ba,ep:item.ep,av:item.av,fr},
    {distMap:win1stDistMap},
    {enableMonteCarlo:false}
  );
  return{...item,pred:p,rec,v2};
}).filter(r=>r.pred);
```

- [ ] **Step 3: dbRows 매핑에 신규 6개 컬럼 추가**

`src/App.jsx:633` 부근의 `dbRows=totalResults.map(r=>({...}))` 객체 끝(`match_status:"pending"` 직전)에 추가:

```js
,bid1st_v2_adj:r.v2?.auto?.adj??null
,bid1st_v2_bid:r.v2?.auto?.bid??null
,bid1st_v2_win_prob:r.v2?.auto?.winProb??null
,bid1st_v2_floor_safe:r.v2?.auto?.floorSafe??null
,bid1st_v2_grain:r.v2?.distribution?.grain??null
,bid1st_v2_src:r.v2?.distribution?.src??null
```

- [ ] **Step 4: useCallback 의존성 배열에 win1stDistMap 추가**

`src/App.jsx:644` (loadPredFiles의 useCallback 종료) `},[allS,bidDetails,agencyPred,floorBench,agAss]);` 를 다음으로 변경:

```js
},[allS,bidDetails,agencyPred,floorBench,agAss,win1stDistMap]);
```

- [ ] **Step 5: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 6: 커밋**

```bash
git add src/App.jsx
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 loadPredFiles에 recommendBid1st 통합

items.map에서 predictV5 + recommendAssumedAdj + recommendBid1st 동시 호출.
dbRows에 bid1st_v2_* 6개 컬럼 매핑 추가. useCallback 의존성에 win1stDistMap 추가.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: `src/App.jsx` — `doManualPred`에 `recommendBid1st` 통합

**Files:**
- Modify: `src/App.jsx:665~673` (`doManualPred` 함수)

- [ ] **Step 1: 현재 doManualPred 코드 확인**

```bash
grep -n "doManualPred\|setManualRec" src/App.jsx | head -10
```

- [ ] **Step 2: state 추가 (manualRec 옆에)**

`src/App.jsx:666` 의 `const[manualRec,setManualRec]=useState(null);` 줄 뒤에 추가:

```js
  const[manualV2,setManualV2]=useState(null); // Phase 23-9: 수동 시뮬레이션의 v2 추천
```

- [ ] **Step 3: doManualPred 함수 변경**

기존 `doManualPred` 코드(line 667~673):

```js
const doManualPred=useCallback(()=>{
  if(!Object.keys(allS.ts||{}).length){setMsg({type:"err",text:"낙찰 데이터가 없습니다. 먼저 데이터를 업로드해주세요."});return}
  const p=predictV5({at:clsAg(inp.agency),agName:inp.agency.trim(),ba:tn(inp.baseAmount),ep:tn(inp.estimatedPrice),av:tn(inp.aValue)},allS.ts,allS.as,bidDetails,agencyPred,floorBench);
  if(!p){setMsg({type:"err",text:"예측 실패: 기관 또는 금액 정보를 확인해주세요."});return}
  setPred(p);if(p)setSimSlider(Math.round(p.adj*100));
  const rec=recommendAssumedAdj({at:clsAg(inp.agency),agName:inp.agency.trim(),ba:tn(inp.baseAmount),ep:tn(inp.estimatedPrice),av:tn(inp.aValue)},allS.ts,allS.as,agAss);
  setManualRec(rec)},[inp,allS,bidDetails,agencyPred,floorBench,agAss]);
```

다음으로 교체:

```js
const doManualPred=useCallback(()=>{
  if(!Object.keys(allS.ts||{}).length){setMsg({type:"err",text:"낙찰 데이터가 없습니다. 먼저 데이터를 업로드해주세요."});return}
  const at=clsAg(inp.agency);
  const agName=inp.agency.trim();
  const ba=tn(inp.baseAmount);
  const ep=tn(inp.estimatedPrice);
  const av=tn(inp.aValue);
  const p=predictV5({at,agName,ba,ep,av},allS.ts,allS.as,bidDetails,agencyPred,floorBench);
  if(!p){setMsg({type:"err",text:"예측 실패: 기관 또는 금액 정보를 확인해주세요."});return}
  setPred(p);if(p)setSimSlider(Math.round(p.adj*100));
  const rec=recommendAssumedAdj({at,agName,ba,ep,av},allS.ts,allS.as,agAss);
  setManualRec(rec);
  // Phase 23-9: v2 추천
  const fr=p.fr;
  const v2=recommendBid1st({at,agName,ba,ep,av,fr},{distMap:win1stDistMap},{enableMonteCarlo:false});
  setManualV2(v2);
},[inp,allS,bidDetails,agencyPred,floorBench,agAss,win1stDistMap]);
```

- [ ] **Step 4: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 5: 커밋**

```bash
git add src/App.jsx
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 doManualPred에 recommendBid1st 통합

수동 시뮬레이션 화면에서 v2 추천을 manualV2 state로 표시할 수 있도록 추가.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: `src/App.jsx` — `getFinalRecommendation` 어댑터화

**Files:**
- Modify: `src/App.jsx:233~277`

- [ ] **Step 1: 현재 getFinalRecommendation 정확한 위치 확인**

```bash
grep -n "getFinalRecommendation=useCallback" src/App.jsx
```

Expected: line 233.

- [ ] **Step 2: 함수 시작부 직후 (line 234, `if(!p)return...` 다음)에 v2 우선 분기 추가**

기존 코드 (line 234~236):

```js
const getFinalRecommendation=useCallback((p)=>{
  if(!p)return{adj:null,bid:null,source:null};
  // LH 종심제/순심제 대형 공사 — 예측 모델 구조적 미지원 (고정 -2.941 수렴)
  if(isLhJongsim(p.at,p.ba,p.pn))return{adj:null,bid:null,bid1st:null,source:'jongsim_unsupported',jongsim:true};
```

다음으로 변경 (`if(isLhJongsim...)` 다음에 v2 분기 삽입):

```js
const getFinalRecommendation=useCallback((p)=>{
  if(!p)return{adj:null,bid:null,source:null};
  // LH 종심제/순심제 대형 공사 — 예측 모델 구조적 미지원 (고정 -2.941 수렴)
  if(isLhJongsim(p.at,p.ba,p.pn))return{adj:null,bid:null,bid1st:null,source:'jongsim_unsupported',jongsim:true};

  // Phase 23-9: bid1st_v2 우선 — 채워진 행은 신규 추천 사용
  if(p.bid1st_v2_adj!=null){
    return{
      adj:Number(p.bid1st_v2_adj),
      bid:Number(p.bid1st_v2_bid),
      bid1st:Number(p.bid1st_v2_bid),  // calcWin1stBid 폐기 — 동일 값
      winProb:p.bid1st_v2_win_prob!=null?Number(p.bid1st_v2_win_prob):null,
      floorSafe:p.bid1st_v2_floor_safe===true,
      source:`v2(${p.bid1st_v2_grain||'기본'}: ${p.bid1st_v2_src||'-'})`
    };
  }

```

(이후 기존 line 237~ 의 `const ba=p.ba?Number(p.ba):0;` 부터 끝까지는 그대로 유지 — 폴백 경로)

- [ ] **Step 3: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 4: 커밋**

```bash
git add src/App.jsx
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 getFinalRecommendation 어댑터화

bid1st_v2_adj가 채워진 행은 신규 추천 사용, 미존재 시 기존 opt_adj 폴백.
시그니처 무변경 — 호출처(App.jsx:1160, 1772, 1965) 그대로 작동.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: `src/App.jsx` — 수동 시뮬레이션 UI 변경 (manualV2 표시)

**Files:**
- Modify: `src/App.jsx:1657~` (수동 시뮬레이션 렌더 영역)

수동 시뮬레이션 화면에 v2 추천을 표시하는 영역 추가. 기존 `pred` 표시는 유지하되 v2가 있으면 그 위에 우선 표시.

- [ ] **Step 1: 현재 수동 시뮬레이션 렌더 위치 확인**

```bash
grep -n "수동 시뮬레이션\|시뮬레이션 결과\|pred&&<div\|manualRec" src/App.jsx | head -10
```

- [ ] **Step 2: pred 결과 영역 위에 v2 영역 추가**

`src/App.jsx:1657` 부근의 `{pred&&<div ...>` 시작 직전에 v2 표시 블록 삽입:

```jsx
{manualV2&&<div style={{marginTop:10,padding:"10px",background:C.bg3,borderRadius:6,border:"1px solid "+C.gold+"33"}}>
  <div style={{fontSize:12,fontWeight:600,color:C.gold,marginBottom:6}}>★ 자동 추천 (1위 확률 최대)</div>
  <div style={{fontSize:11,color:C.txm,marginBottom:4}}>
    분포: {manualV2.distribution.src} (grain: {manualV2.distribution.grain||'-'}) ·
    평균 {(100+manualV2.distribution.mean).toFixed(4)}% · σ {manualV2.distribution.std.toFixed(4)}%
  </div>
  <div style={{padding:"8px 10px",background:C.bg,borderRadius:4,marginBottom:8}}>
    <div style={{fontSize:18,fontWeight:700,color:C.gold,fontFamily:"monospace"}}>{(100+manualV2.auto.adj).toFixed(4)}%</div>
    <div style={{fontSize:14,fontWeight:700,fontFamily:"monospace"}}>{tc(manualV2.auto.bid)}원</div>
    <div style={{fontSize:11,marginTop:4}}>
      <span style={{color:manualV2.auto.floorSafe?"#5dca96":"#e24b4a",fontWeight:600}}>{manualV2.auto.floorSafe?"[자격OK]":"[자격미달]"}</span>
      <span style={{color:C.txm,marginLeft:8}}>1위확률 {Math.round(manualV2.auto.winProb*100)}%</span>
    </div>
  </div>
  <div style={{fontSize:11,color:C.txm,marginBottom:4}}>3구간 옵션</div>
  {[['적극(P25)','aggressive'],['균형(P50)','balanced'],['보수(P75)','conservative']].map(([label,key])=>{
    const o=manualV2.scenarios[key];
    return<div key={key} style={{display:"grid",gridTemplateColumns:"60px 1fr 1fr 80px",gap:6,fontSize:11,padding:"3px 0"}}>
      <span style={{color:C.txd}}>{label}</span>
      <span style={{fontFamily:"monospace"}}>{(100+o.adj).toFixed(4)}%</span>
      <span style={{fontFamily:"monospace",color:C.txm}}>{tc(o.bid)}원</span>
      <span style={{color:o.floorSafe?"#5dca96":"#e24b4a",fontSize:10}}>{o.label}</span>
    </div>;
  })}
</div>}

```

이 블록을 `{pred&&<div ...>` 바로 직전에 삽입.

- [ ] **Step 3: 빌드 통과 확인**

```bash
npx vite build 2>&1 | tail -10
```

Expected: `✓ built in X.XXs`.

- [ ] **Step 4: 커밋**

```bash
git add src/App.jsx
git commit -m "$(cat <<'EOF'
feat(predict): Phase 23-9 수동 시뮬레이션 UI에 v2 추천 표시

자동 추천(1위확률 최대 위치) + 3구간(적극/균형/보수) + 적격성 라벨 + 분포 정보.
기존 pred 표시는 그대로 유지 (폴백 가시화).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: 통합 검증 — dev server + DB 채워짐 확인

**Files:** (검증 only)

- [ ] **Step 1: dev server 시작**

```bash
npx vite
```

브라우저로 `http://localhost:5173` 열기. 인증 통과(로그인) 후 예측탭 진입.

- [ ] **Step 2: 수동 시뮬레이션 호출**

예측탭 우측 "빠른 시뮬레이션" 영역에서:
- 발주기관: `한국전력공사` 또는 자주 쓰는 발주사 입력
- 기초금액: `200000000`
- A값: `0` (또는 적절한 값)
- "시뮬레이션" 버튼 클릭

Expected: "★ 자동 추천 (1위 확률 최대)" 영역이 새로 보임. `(100+adj).toFixed(4)` 형식으로 4자리 정밀도 표시. 적격성 라벨과 1위확률 % 표시. 3구간(적극/균형/보수) 표시.

- [ ] **Step 3: 입찰서류함 dummy 업로드 → DB 컬럼 채워짐 확인**

기존 dummy 입찰서류함 XLS가 있다면 예측탭 좌측 업로드 영역으로 드래그. 없으면 이 step skip.

업로드 후 Supabase MCP로 확인:

```sql
SELECT id, pn, ag, bid1st_v2_adj, bid1st_v2_bid, bid1st_v2_win_prob,
       bid1st_v2_floor_safe, bid1st_v2_grain, bid1st_v2_src, created_at
FROM bid_predictions
WHERE source='file_upload'
  AND created_at >= NOW() - INTERVAL '5 minutes'
ORDER BY created_at DESC
LIMIT 5;
```

Expected: 신규 6개 컬럼이 채워진 행. `bid1st_v2_grain`이 `AG_BA`/`AG`/`AT_BA`/`AT` 중 하나 또는 `null`(셀 미달).

- [ ] **Step 4: dev server 종료 + 결과 기록**

```bash
# Ctrl+C로 dev server 종료
echo "Task 14 결과: 자동 추천 화면 표시 OK / DB 컬럼 채워짐 OK"
```

---

## Task 15: Phase 23-3 게이트 — predict-architect 사전 평가

**Files:** (검증 only)

- [ ] **Step 1: predict-architect 서브에이전트 호출**

다음 prompt로 `Agent` 호출 (subagent_type=`predict-architect`):

```
Phase 23-9 1단계 push 직전 사전 평가 요청.

변경 내용:
- src/lib/utils.js: recommendBid1st, lookupWin1stDist, calcWinProb, baSegOf 신규
- src/lib/supabase.js: sbFetchWin1stDistMap 신규
- src/App.jsx: state·fetch·loadPredFiles·doManualPred·getFinalRecommendation·UI 변경
- DB: bid_predictions 6개 컬럼 ALTER, win1st_dist_map VIEW, bid1st_v2_validation VIEW

분류 예상: Generator (recommendBid1st 메인 추천 + getFinalRecommendation 어댑터화)
opt_adj/pred_adj_rate는 무변경 — mae 회귀 0건 기대.

핵심 영역(한전·고양시·군부대) 영향:
- 셀 커버리지 확인 후 진행 (Task 2 Step 3에서 30%+ 통과 기준 적용)
- bid1st_v2_adj가 채워진 행만 신규 추천 사용, NULL 행은 기존 폴백

200단어 이내 PROCEED/REVISE/ABORT 권고 부탁.
프로젝트: sadunejfkstxbxogzutl, 디렉토리: C:\Users\home\bid-analyzer
```

Expected: PROCEED 권고 수신. REVISE/ABORT 시 STOP — 권고 사항 반영 후 plan 수정.

- [ ] **Step 2: 결과 확인 후 진행/중단 결정**

PROCEED 시 다음 Task로. REVISE 시 권고 항목별 plan 수정 후 해당 Task 재실행. ABORT 시 plan 전체 보류.

---

## Task 16: `/evaluate` 회귀 검증

**Files:** (검증 only)

- [ ] **Step 1: /evaluate 슬래시 커맨드 실행**

```
/evaluate
```

또는 사용자가 직접 실행. 명령은 `evaluate_model_release()` DB 함수 호출 + 핵심 영역 MAE 측정.

- [ ] **Step 2: PASS/WARN/FAIL 판정 확인**

Expected:
- `mae`: baseline 동일 또는 차이 < 0.001 (opt_adj 무변경이라 자연 PASS)
- `floor_safe_pct`, `hit_0_5_pct`: baseline 동일
- 핵심 영역 한전·고양시·군부대 MAE 회귀 없음

PASS 시 다음 Task. WARN 시 사용자 동의 후 진행. FAIL 시 STOP — 원인 분석.

---

## Task 17: deploy-gate + main push

**Files:** (배포 only)

- [ ] **Step 1: deploy-gate 서브에이전트 호출**

다음 prompt로 `Agent` 호출 (subagent_type=`deploy-gate`):

```
Phase 23-9 1단계 push 직전 통합 게이트 요청.

변경 분류: Generator (recommendBid1st 메인 추천 + getFinalRecommendation 어댑터화).
opt_adj/pred_adj_rate 무변경 → mae·floor_safe_pct·hit_0_5_pct baseline 동일 기대.

빌드 OK (npx vite build 통과 — Task 4·5·6·7·9·10·11·12·13에서 매번 확인).
predict-architect PROCEED 받음 (Task 15).
/evaluate PASS 받음 (Task 16).

git status 확인 + 핵심 영역(한전·고양시·군부대) MAE 검증 후 PASS/FAIL 판정 부탁.
프로젝트: sadunejfkstxbxogzutl
```

Expected: PASS 판정. FAIL 시 STOP.

- [ ] **Step 2: git status 확인**

```bash
git status && git log --oneline -10
```

Expected: working tree clean (모든 task에서 commit 완료). 신규 commit 9~10개.

- [ ] **Step 3: pull --rebase 후 push**

```bash
git pull --rebase origin main && git push origin main
```

Expected: `main -> main` 출력. Vercel 자동 배포 트리거 (2~3분 소요).

- [ ] **Step 4: 배포 확인**

```bash
echo "Vercel 배포 확인: https://bid-analyzer-pi.vercel.app (2~3분 후 신규 추천 활성)"
```

배포 완료 후 production에서 한 번 더 수동 시뮬레이션 호출 → "★ 자동 추천 (1위 확률 최대)" 영역 표시 확인.

- [ ] **Step 5: 1단계 측정 시작 일자 기록**

```sql
INSERT INTO project_milestones (phase, event, occurred_at, note)
VALUES ('23-9', '1단계 배포', NOW(), 'recommendBid1st A 통계 추정 + UI 노출. 4주 측정 시작.')
ON CONFLICT DO NOTHING;
```

(만약 `project_milestones` 테이블이 없으면 이 step skip — 메모리에 기록)

---

## Task 18: 1단계 측정 추적 (운영, plan 외부)

이 plan 범위 밖. 별도 plan으로 분리:

- 측정 기간: 1단계 배포 후 4주
- 측정 metric: `bid1st_v2_validation` VIEW의 `closer_than_winner_pct`, `floor_safe_pct`, `bid1st_pct_gap_p50`, win_prob calibration
- 통과 기준 (spec 9.1): closer ≥ 85%, floor_safe ≥ 22%, calibration ±10%p
- 통과 시: 2단계 (Monte Carlo 보강) plan 작성
- 실패 시: spec 11.3 운영 위험 시나리오 적용 → grain 조정 또는 ABORT

별도 plan 파일: `docs/superpowers/plans/2026-05-26-bid1st-v2-phase1-evaluation.md` (1단계 배포 4주 후 작성)

---

## 자체 검토 체크리스트

이 plan 작성 후 spec 대조 결과:

| spec 요구 | plan task |
|---|---|
| 1.1 ALTER bid_predictions 6개 컬럼 | Task 1 |
| 1.2 win1st_dist_map VIEW | Task 2 |
| 1.3 bid1st_v2_validation VIEW | Task 3 |
| 1.4 sbFetchWin1stDistMap | Task 7 |
| 1.5 sbSavePredictions 매핑 | Task 8 (no-op) + Task 10 (실제 매핑) |
| 1.6 recommendBid1st | Task 6 |
| 1.7 lookupWin1stDist · calcWinProb | Task 4·5 |
| 1.8 App.jsx 통합 | Task 9·10·11 |
| 1.9 getFinalRecommendation 어댑터화 | Task 12 |
| 1.10 UI 변경 | Task 13 (수동 시뮬레이션) — 통합 예측 리스트 UI는 1단계 단순화 위해 다음 plan으로 분리 |
| Phase 23-3 게이트 | Task 15·16·17 |

**식별된 gap**: spec 1.10에 "통합 예측 리스트 UI" 변경도 포함되어 있지만, 이 plan에선 수동 시뮬레이션 UI(Task 13)만 다룸. 이유:
- 통합 예측 리스트는 `getFinalRecommendation` 어댑터화(Task 12)로 자동 신규 추천 표시됨 (별도 UI 변경 불필요)
- 색상 라벨은 `getFinalRecommendation` 결과의 `floorSafe`/`winProb` 활용해 향후 추가 가능 — 1단계 후 측정 결과 보고 결정

**Placeholder scan**: 모든 step에 실제 코드/명령/expected 출력 명시. TBD/TODO 없음.

**Type consistency**: `recommendBid1st` 반환 객체의 `auto.adj/bid/winProb/floorSafe`, `scenarios.{aggressive,balanced,conservative}`, `distribution.{grain,n,mean,std,src,monteCarloUsed}` — Task 6 정의 → Task 10·11·13에서 동일 키 사용. 일관 확인.

---

**Plan 끝.**
