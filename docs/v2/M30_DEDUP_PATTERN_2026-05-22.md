# m30 113건 dedup_key 우회 패턴 분석 — 종결 (2026-05-22)

## 1. 배경
`M32_DIAGNOSIS_2026-05-22.md` §5.1 후속 권고 — `is_duplicate=true`로 마킹된 m30의 113건이 어떻게 `dedup_key` UNIQUE 제약을 우회해 INSERT됐는지 패턴 분류.

## 2. dedup_key 정의 (`upsert_bid_record`)
```sql
v_dedup := v_pn_no || '-' || v_ord || '-' || v_clsfc || '-' || v_rbid;
```
| 토큰 | 의미 | 기본값 |
|---|---|---|
| `v_pn_no` | bidNtceNo | (필수) |
| `v_ord` | bidNtceOrd | `'000'` |
| `v_clsfc` | bidClsfcNo | `'0'` |
| `v_rbid` | rbidNo (재공고번호) | `'000'` |

## 3. 진단 결과

### 3.1 차이 컬럼 분류 (113건)
| 차이 패턴 | n | 비율 |
|---|---|---|
| ag/od/ba 모두 동일, **dedup_key만 다름** | **108** | 95.6% |
| ba만 다름 | 5 | 4.4% |
| ag 다름 | 0 | — |
| od 다름 | 0 | — |
| 짝 없음 (orphan) | 0 | — |

→ 108건은 ag/od/ba 동일하면서 `rbidNo` 토큰만 다른 케이스.

### 3.2 dedup_key 샘플
| dup_dedup | keep_dedup | 차이 |
|---|---|---|
| `R26BK01482785-000-0-001` | `R26BK01482785-000-0-002` | rbidNo 001↔002 |
| `R26BK01447045-000-0-001` | `R26BK01447045-000-0-002` | rbidNo 001↔002 |
| `R26BK01476062-000-0-000` | `R26BK01476062-000-0-001` | rbidNo 000↔001 |

### 3.3 데이터 채움 상태
| 항목 | dup 113건 | keep 짝 113건 |
|---|---|---|
| ar1 채움 | 0 | 0 |
| br1 채움 | 0 | 0 |
| bp 채움 | 5 | 1 |
| **양쪽 모두 NULL** | **108** | **108** |

→ **양쪽 모두 빈 record**. 인포21c가 같은 공고를 rbidNo만 다르게 두 번 발행한 케이스 — 사정률·낙찰가율·낙찰가 데이터 없음.

## 4. 결론

### 4.1 원인 분류
- **무결성 결함이 아닌 외부 시스템(인포21c) 발행 특성**
- 같은 공고가 rbidNo=000/001 또는 001/002로 중복 발행
- 두 row 모두 빈 데이터 (개찰 결과 없음, 미낙찰 또는 입력 미반영)

### 4.2 영향 평가
- m30에서 `is_duplicate=true`로 마킹 완료 → 학습/통계에서 제외됨
- 빈 record라 가중치 0 → 현재 영향 사실상 없음
- 향후 동일 패턴이 새로 들어오면 빈 record가 누적될 수 있으나, 마킹 안 해도 통계에 영향 없음

### 4.3 적용 작업 — 없음
- DB 마이그레이션 미적용
- 코드 변경 없음
- 영구 기록만 남기고 종결

## 5. 검토된 대안 (모두 선택 안 함)

### 5.1 O2: dedup_key 정의에서 rbidNo 제외
- 효과: 향후 동일 pn_no+ord+clsfc 중복 자동 차단
- **위험 큼**: 기존 `upsert_bid_record` ON CONFLICT 정책 변경 → 정상 재공고 차수도 차단 가능
- 군부대 발주 등 일부 정상 다중 차수 데이터 손상 우려
- 선택 안 함

### 5.2 O3: 자동 마킹 트리거
- 효과: 빈 record 중복 INSERT 시 자동 `is_duplicate=true`
- 위험 작지만 빈 record 마킹은 학습 가중치 0이라 효과도 작음
- 우선순위 낮음, 별도 commit 후보

## 6. Phase 23-3 게이트
| 게이트 | 결과 |
|---|---|
| Generator 분류? | **No** (작업 자체 없음, 진단만) |
| Evaluator 분류 | N/A |
| 적용 마이그레이션 | 0건 |
| deploy-gate | 면제 |

## 7. 관련 문서
- `M32_DIAGNOSIS_2026-05-22.md` §5.1 — 본 후속 작업의 권고 출처
- `M30_APPLY_RESULT_2026-05-21.md` — m30 113건 원래 마킹 작업

---
_처리자: Claude Opus 4.7 / 처리 일자: 2026-05-22 / 종결: 적용 없음, 영구 기록만_
